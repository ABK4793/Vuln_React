capture:
  build-command-inference: false


analyze:
  aggressiveness-level: high
  # location

  checkers:
    all-security: true
    audit: true
    distrust-all: true
    sigma:
      enable-check-set: ["all"]
    webapp-security:
      enabled: true
